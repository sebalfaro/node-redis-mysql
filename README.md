## NODE - REDIS - MYSQL - PHPMYADMIN  APP

** node_modules update**
Running the following command should update the node_modules folder inside the container

```sh
docker-compose run app npm install 
```
